describe("TodoModal", () => {
    it("is given pre-transition classes on mount");
    it("removes pre-transition classes after a delay");
    it("displays the title value from a given prop");
    it("changes the title value");
    it("displays the details value from a given prop");
    it("changes the details value");
    it("displays the tag value from a given prop");
    it("changes the tag value");
    it("creates a task");
    it("edits a task");
    it("sets an error when given a false return value");
    it("displays an error");
    it("displays the 'delete' button if editing");
    it("closes the modal");
    it("resets errors upon closing");
})