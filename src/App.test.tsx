describe("App", () => {
    it("displays the create/edit modal");
    it("displays the welcome modal");
    it("sets inert on other elements if a modal is open");
    it("removes inert when a modal is closed");
    it("filters todos based on the filter value");
    it("provides the correct todos for each column based on status");
})